# configuration file for development environment

# the logger engine to use
# console: log messages to STDOUT (your console where you started the
#          application server)
# file:    log message to a file in log/
logger: "file"

# the log level for this environment
# core is the lowest, it shows Dancer2's core log messages as well as yours
# (debug, info, warning and error)
log: "core"

# should Dancer2 consider warnings as critical errors?
warnings: 1

# should Dancer2 show a stacktrace when an error is caught?
show_errors: 1

# print the banner
startup_info: 1

# OfBi stuff
# URL prefix of static files like images, css or js files. The app itself serves
# the static files. Defaults to empty.
static_prefix: '/fcgi-bin/dispatch.fcgi'

# List of index files listing chapter files. At least one is required.
indexes:
  - "~/webResults/generated.index"

